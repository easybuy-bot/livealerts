# Face-detection model

Place an OpenCV-DNN compatible face-detection model here as `face.onnx`
(for example an OpenCV Zoo YuNet model, or a ResNet-SSD face detector).

If no model is present the worker still runs and applies the fixed privacy
zones; only automatic face blur is disabled.
